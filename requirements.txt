# Pinned runtime dependencies, mirroring [project.dependencies] in pyproject.toml.
# Airflow and dbt are intentionally excluded here: the Docker image provides
# Airflow from its base image, and dbt is installed into a separate virtualenv.
# See Dockerfile for how each layer is installed.
pandas==2.1.4
psycopg2-binary==2.9.10
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.10.6
pydantic-settings==2.7.1
SQLAlchemy==1.4.54
openai==1.59.6
